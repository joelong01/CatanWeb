<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Catan3.Controls.HarborCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Catan3.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="HarborPathStyle" TargetType="Path">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Stroke" Value="{StaticResource bmMaple}" />
            <Setter Property="Canvas.ZIndex" Value="-1" />
            <Setter Property="StrokeThickness" Value="4" />
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
    </UserControl.Resources>
    <Grid BorderBrush="Yellow" BorderThickness="0">
        
        <Ellipse Width="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}"
                Height="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}"
                Fill="{x:Bind Bind_HarborImage(ViewModel.Harbor.HarborType), Mode=OneWay}">

        </Ellipse>
        

    </Grid>
</UserControl>
