<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Catan3.Controls.HarborCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Catan3.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="HarborPathStyle" TargetType="Path">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Stroke" Value="{StaticResource bmMaple}" />
            <Setter Property="Canvas.ZIndex" Value="-1" />
            <Setter Property="StrokeThickness" Value="4" />
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
    </UserControl.Resources>
    <Grid>

        <Ellipse Width="50" Height="50" Fill="Green">
            <Ellipse.RenderTransform>
                <CompositeTransform TranslateX="-40" />
            </Ellipse.RenderTransform>
        </Ellipse>
        <Path  Grid.RowSpan="3" Grid.ColumnSpan="3"
                Style="{StaticResource HarborPathStyle}" Visibility="Visible">
            <Path.Clip>
                <RectangleGeometry Rect="8 0 100 100"  />
            </Path.Clip>
            <Path.Data>
                <PathGeometry>
                    <PathGeometry.Figures>
                        <PathFigureCollection>
                            <PathFigure StartPoint="48 0">
                                <PathFigure.Segments>
                                    <LineSegment Point="48 50" />
                                </PathFigure.Segments>
                            </PathFigure>

                            <PathFigure StartPoint="9 16">
                                <PathFigure.Segments>
                                    <LineSegment Point="9 34" />
                                </PathFigure.Segments>
                            </PathFigure>

                            <PathFigure StartPoint="48 0">
                                <PathFigure.Segments>
                                    <PathSegmentCollection>
                                        <ArcSegment IsLargeArc="True"
                                                Point="2 0" Size="1 1"
                                                SweepDirection="Clockwise" />
                                    </PathSegmentCollection>
                                </PathFigure.Segments>
                            </PathFigure>
                            <PathFigure StartPoint="48 50">
                                <PathFigure.Segments>
                                    <PathSegmentCollection>
                                        <ArcSegment IsLargeArc="True"
                                                Point="2 50" Size="3 3"
                                                SweepDirection="Counterclockwise" />
                                    </PathSegmentCollection>
                                </PathFigure.Segments>
                            </PathFigure>
                        </PathFigureCollection>
                    </PathGeometry.Figures>
                </PathGeometry>
            </Path.Data>
        </Path>
        
    </Grid>
</UserControl>
