<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Catan3.Controls.HarborCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:u="using:Catan3.Utility"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="HarborPathStyle" TargetType="Path">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Stroke" Value="{StaticResource bmMaple}" />
            <Setter Property="Canvas.ZIndex" Value="-1" />
            <Setter Property="StrokeThickness" Value="4" />
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
    </UserControl.Resources>
    <Border BorderBrush="Yellow" BorderThickness="0">
        <Canvas Name="LayoutRoot"
                Width="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}"
                Height="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}">
            <Polygon Points="{x:Bind ViewModel.HarborPoints, Mode=OneWay}"
                    Canvas.ZIndex="-1" IsHitTestVisible="False"
                    Fill="{StaticResource bmWater}" StrokeThickness="0" />
            <u:FlipperCtrl
                    Orientation="{x:Bind ViewModel.Orientation, Mode=OneWay}">
                <u:FlipperCtrl.Back>
                    <Ellipse Fill="{StaticResource bmWater}"
                            Width="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}"
                            Height="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}" />
                </u:FlipperCtrl.Back>
                <u:FlipperCtrl.Front>
                    <Ellipse
                            Width="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}"
                            Height="{x:Bind ViewModel.Layout.BuildingSize, Mode=OneWay}"
                            Fill="{x:Bind Bind_HarborImage(ViewModel.Harbor.HarborType), Mode=OneWay}" />
                </u:FlipperCtrl.Front>
            </u:FlipperCtrl>

        </Canvas>
    </Border>
</UserControl>
