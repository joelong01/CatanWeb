
<UserControl x:Class="Catan3.Controls.GameButtons"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Catan3.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="using:Catan3.Models"
        d:DataContext="{d:DesignInstance Type=models:GameViewModel}"
        mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Width="100" Height="100" Content="Undo" Padding="10"
                    Command="{x:Bind ViewModel.UndoCommand, Mode=OneWay}" />
            <Button Width="100" Height="100" Content="Next"
                    Command="{x:Bind ViewModel.NextPlayerCommand, Mode=OneWay}" />
            <!--<Button Width="100" Height="100" Content="Redo"
                    Command="{x:Bind ViewModel.RedoCommand, Mode=OneWay}" />-->
        </StackPanel>
    </Grid>
</UserControl>
