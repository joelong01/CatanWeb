<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Catan3.Controls.SetPlayerOrderCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Catan3.Controls" xmlns:models="using:Catan3.Models"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Grid IsHitTestVisible="True">

        <ListView x:Name="PlayersListView" CanReorderItems="True"
                CanDragItems="True" AllowDrop="True" CornerRadius="5"
               
                ItemsSource="{x:Bind ViewModel.Players, Mode=OneWay}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid HorizontalAlignment="Stretch" MaximumRowsOrColumns="5"
                            Orientation="Horizontal" />

                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="4" />
                    <Setter Property="HorizontalAlignment" Value="Left" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:PlayerOrderModel">

                    <StackPanel CornerRadius="5" BorderBrush="{x:Bind PlayerColors.ForegroundBrush}" BorderThickness="1">
                        
                        <Button Content="Go First" Margin="10"
                                VerticalAlignment="Top"
                                HorizontalAlignment="Center"
                                Tag="{x:Bind}"
                                Click="GoFirstClick" />
                        <Ellipse Width="75" Height="75" Margin="10">
                            <Ellipse.Fill>
                                <ImageBrush
                                        ImageSource="{x:Bind CroppedImageUri, Mode=OneWay}" />
                            </Ellipse.Fill>
                        </Ellipse>
                        <TextBlock Text="{x:Bind Name}" Foreground="White" Margin="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Bottom" />
                    </StackPanel>

                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
