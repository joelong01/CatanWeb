<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Catan3.Controls.PlayerCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="using:Catan3.Controls" xmlns:models="using:Catan3.Models"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Grid Margin="25">
        <Grid BorderBrush="Black" BorderThickness="2" Height="200">

            <StackPanel HorizontalAlignment="Stretch" Background="DarkKhaki">
                <StackPanel HorizontalAlignment="Stretch"
                        Orientation="Horizontal" Background="AliceBlue"
                        Height="50">

                </StackPanel>
                <c:TrackedResourcesCtrl ResourcesViewModel="{x:Bind PlayerViewModel.ResourcesThisTurn, Mode=OneWay}"/>
                <StackPanel HorizontalAlignment="Stretch"
                        Orientation="Horizontal" Background="AliceBlue"
                        Height="50">

                </StackPanel>
            </StackPanel>

            <Ellipse HorizontalAlignment="Left" VerticalAlignment="Top"
                    Width="50" Height="50" Margin="-25, -25, 0, 0"
                    Fill="{x:Bind PlayerViewModel.GetBrush(PlayerViewModel.Background, x:False), Mode=OneWay}" />
        </Grid>
    </Grid>
</UserControl>
