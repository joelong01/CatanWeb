<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Catan3.Controls.PlayerCounterCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Catan3.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        x:Name="UserControl" mc:Ignorable="d">

    <Grid Height="50" Width="50"
            Background="{Binding ElementName=UserControl, Path=Background, Mode=OneWay}"
            CornerRadius="10">
        <Viewbox Stretch="Uniform" HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <TextBlock
                    FontFamily="{Binding ElementName=UserControl, Path=FontFamily, Mode=OneWay}"
                    Text="{x:Bind Glyph, Mode=OneWay}"
                    Foreground="{Binding ElementName=UserControl, Path=Foreground, Mode=OneWay}" />

        </Viewbox>
        <Viewbox Height="20" HorizontalAlignment="{x:Bind CountHorizontalAlignment, Mode=OneWay}"
                VerticalAlignment="{x:Bind CountVerticalAlignment, Mode=OneWay}" Stretch="Uniform">
            <Grid  Background="{Binding ElementName=UserControl, Path=Background, Mode=OneWay}" CornerRadius="10" Margin="0">
              
                <TextBlock Text="{x:Bind Count, Mode=OneWay}" Foreground="{Binding ElementName=UserControl, Path=Foreground, Mode=OneWay}" Margin="2"
                        TextLineBounds="Tight" />

            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
