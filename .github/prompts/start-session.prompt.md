---
name: start-session
description: Begin a new coding session using the shared start-session spec.
agent: agent
argument-hint: "Optional: additional context for this work session"
---

#files:"./.ai/commands/start-session.md"

You will load and follow the canonical session-start instructions
contained in the file above.

If the user provides additional context after invoking /start-session,
treat that as “extra incoming session context” and incorporate it at the
top of the session plan you generate.

Follow the instructions exactly as written in the file.
