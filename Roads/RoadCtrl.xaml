<UserControl x:Class="Catan3.Controls.RoadCtrl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:c="using:Catan3.Controls" xmlns:models="using:Catan3.Models"
        d:DataContext="{d:DesignInstance Type=models:RoadViewModel}"
        mc:Ignorable="d" d:DesignHeight="12" d:DesignWidth="70.8">
    <UserControl.Resources>
        <Style x:Key="RoadLineStyle" TargetType="Line">
            <Setter Property="StrokeThickness" Value="2" />
            <Setter Property="StrokeEndLineCap" Value="Triangle" />
            <Setter Property="StrokeStartLineCap" Value="Triangle" />
        </Style>
    </UserControl.Resources>

    <Canvas Background="Transparent" HorizontalAlignment="Left"
            VerticalAlignment="Top" Canvas.ZIndex="100" IsHitTestVisible="True"
            RenderTransformOrigin=".5 .5">

        <Polygon
                Fill="{x:Bind ViewModel.GetBackgroundBrush(ViewModel.Road.RoadState, ViewModel.Road.OwnerId, ViewModel.CurrentPlayer), Mode=OneWay}"
                Points="{x:Bind ViewModel.RoadPolygon, Mode=OneWay}"
                Stroke="{x:Bind ViewModel.GetForegroundBrush(ViewModel.Road.RoadState, ViewModel.Road.OwnerId, ViewModel.CurrentPlayer), Mode=OneWay}"
                StrokeThickness="{x:Bind ViewModel.Layout.RoadStrokeThickness, Mode=OneWay}"
                Opacity="{x:Bind ViewModel.Opacity(ViewModel.Road, ViewModel.Road.RoadState), Mode=OneWay}"
              
                PointerPressed="OnPointerPressed" />

        <Ellipse Width="10" Height="10" Fill="Green" Visibility="Collapsed" />
        <Viewbox Stretch="Uniform" Height="20" HorizontalAlignment="Left"
                VerticalAlignment="Top" IsHitTestVisible="False"
                Canvas.Left="{x:Bind ViewModel.RoadCenter.X, Mode=OneWay}"
                Canvas.Top="{x:Bind ViewModel.RoadCenter.Y, Mode=OneWay}">
            <Grid Background="Black" Height="20" Width="20" CornerRadius="5" IsHitTestVisible="False"
                    HorizontalAlignment="Left" VerticalAlignment="Top"
                    Visibility="{x:Bind ViewModel.ShowBuildIndex(ViewModel.Road.BuildIndex), Mode=OneWay}">
                <TextBlock
                        Text="{x:Bind ViewModel.BuildIndex(ViewModel.Road.BuildIndex), Mode=OneWay}"
                        FontSize="16" HorizontalAlignment="Center" TextLineBounds="Tight" IsHitTestVisible="False"
                        VerticalAlignment="Center" Foreground="White" />
            </Grid>
        </Viewbox>

    </Canvas>

</UserControl>
