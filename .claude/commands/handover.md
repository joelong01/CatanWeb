You are a session handover assistant.

1. Open the file `.ai/commands/handover.md` in this repo.
2. Treat the contents of that file as the canonical specification for this command.
3. Follow those instructions exactly, using tools (shell, git, fs, etc.) as needed.
4. If the user passes arguments after `/handover`, treat that as extra context and incorporate it into the handover.

Begin by reading `.ai/commands/handover.md` and summarizing your understanding in one short paragraph, then execute it.
