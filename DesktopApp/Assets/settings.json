{
  "settings": [
    {
      "settingName": "SaveFileLocation",
      "description": "Directory to save game files",
      "inputType": "textbox",
      "value": "",
      "defaultValue": "",
      "tooltip": "Choose where game save files will be stored on your computer",
      "errorTooltip": "Please select a valid directory that exists on your system",
      "validation": {
        "required": true,
        "directoryMustExist": true
      }
    },
    {
      "settingName": "ExpansionGoldTiles",
      "description": "Number of gold tiles in expansion games",
      "inputType": "dropdown",
      "options": ["0", "1", "2", "3", "4"],
      "value": 2,
      "defaultValue": 2,
      "tooltip": "Gold tiles produce any resource when rolled. Expansion games support 0-4 gold tiles",
      "errorTooltip": "Please select a valid number of gold tiles",
      "validation": {
        "required": true
      }
    },
    {
      "settingName": "RegularGoldTiles",
      "description": "Number of gold tiles in regular games",
      "inputType": "dropdown",
      "options": ["0", "1", "2"],
      "value": 1,
      "defaultValue": 1,
      "tooltip": "Gold tiles produce any resource when rolled. Regular games support 0-2 gold tiles",
      "errorTooltip": "Please select a valid number of gold tiles",
      "validation": {
        "required": true
      }
    },
    {
      "settingName": "ExpansionForLessThanFivePlayers",
      "description": "Enable expansion mode for games with less than 5 players",
      "inputType": "checkbox",
      "value": false,
      "defaultValue": false,
      "tooltip": "When enabled, expansion board and rules are used even with fewer than 5 players",
      "errorTooltip": "",
      "validation": {
        "required": false
      }
    },
    {
      "settingName": "ServiceGame",
      "description": "Use GameService instead of local game logic",
      "inputType": "checkbox",
      "value": true,
      "defaultValue": true,
      "tooltip": "When enabled, game logic runs on a remote server. When disabled, game runs locally",
      "errorTooltip": "",
      "validation": {
        "required": false
      }
    },
    {
      "settingName": "GameServiceUrl",
      "description": "URL of the GameService (e.g., http://localhost:8080)",
      "inputType": "textbox",
      "value": "http://localhost:8080",
      "defaultValue": "http://localhost:8080",
      "tooltip": "The address of the GameService server. Must be reachable when ServiceGame is enabled",
      "errorTooltip": "GameService is not reachable at this address. Please check the URL and ensure the service is running",
      "validation": {
        "required": true
      }
    }
  ]
}