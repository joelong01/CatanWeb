<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx x:Class="Catan3.DebugWindow"
        xmlns:winex="using:WinUIEx"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Catan Debug Messages"
        Width="800"
        Height="600"
        IsResizable="True"
        IsTitleBarVisible="True"
        PersistenceId="Catan_DebugWindow"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Debug Messages" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="10,10,10,5"/>
        
        <!-- Messages TextBox -->
        <ScrollViewer Grid.Row="1" 
                      Margin="10,5,10,5"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="MessagesTextBox"
                     FontFamily="Consolas"
                     FontSize="12"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     BorderThickness="1">
                <TextBox.Background>
                    <SolidColorBrush Color="#FF1E1E1E"/>
                </TextBox.Background>
                <TextBox.Foreground>
                    <SolidColorBrush Color="#FFD4D4D4"/>
                </TextBox.Foreground>
                <TextBox.BorderBrush>
                    <SolidColorBrush Color="#FF3F3F46"/>
                </TextBox.BorderBrush>
            </TextBox>
        </ScrollViewer>
        
        <!-- Footer with Copy and Clear buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Vertical" 
                    Margin="10,5,10,10">
            <!-- Controls Row -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,0,0,10"
                        Spacing="10">
                <!-- Truncate Lines Checkbox and Input -->
                <StackPanel Orientation="Horizontal" 
                            Spacing="8"
                            VerticalAlignment="Center">
                    <CheckBox x:Name="TruncateCheckBox"
                              Content="Truncate Lines:"
                              IsChecked="True"
                              Checked="OnTruncateToggled"
                              Unchecked="OnTruncateToggled"/>
                    <TextBox x:Name="TruncateLimitBox"
                             Text="50"
                             Width="60"
                             IsEnabled="False"
                             InputScope="Number"
                             VerticalAlignment="Center"/>
                    <TextBlock Text="messages"
                               VerticalAlignment="Center"
                               FontSize="12"/>
                </StackPanel>
                
                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            Spacing="5">
                    <Button x:Name="CopyButton" 
                            Content="Copy" 
                            Click="OnCopyClick"
                            MinWidth="75"/>
                    <Button x:Name="ClearButton" 
                            Content="Clear" 
                            Click="OnClearClick"
                            MinWidth="75"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</winex:WindowEx>
